{
  "name": "campus-connect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "free:4000": "node scripts/free-port.mjs 4000",
    "dev": "node scripts/free-port.mjs 4000 && concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "node scripts/free-port.mjs 4000 && npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
