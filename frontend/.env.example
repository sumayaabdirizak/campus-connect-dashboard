# ── Frontend environment ─────────────────────────────────────────────
# Copy to `.env.local` and adjust. Public (NEXT_PUBLIC_*) vars are exposed
# to the browser — never put secrets here.

# Backend base URL for REST + Socket.IO (default: http://localhost:4000)
NEXT_PUBLIC_API_URL="http://localhost:4000"
