'use client';
import React from 'react';

const ROLES = ['STUDENT', 'TEACHER', 'DEAN', 'SUPER_ADMIN'];

export function UsersTabs() {
  return (
    <div className='p-4 text-center text-muted-foreground'>
      User management is available through the navigation.
    </div>
  );
}
