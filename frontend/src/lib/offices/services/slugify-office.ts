/** Lowercase slug from a display name (letters, digits, hyphens). */
export function slugifyOfficeName(name: string): string {
  return name
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
    .slice(0, 48);
}
